import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles (theme =>({
    wrapper:{
        justifyContent: 'center',
        color:'red',
        fontSize:'40px',
        textAlign:'center'
    },
    main: {
        fontSize:'50px',
    }
}));